repos:
  - repo: local
    hooks:
      - id: stylua
        name: stylua
        entry: stylua --check
        language: system
        types: [lua]
        pass_filenames: true

      - id: luacheck
        name: luacheck
        entry: luacheck
        language: system
        types: [lua]
        pass_filenames: true
