--- morg-mode.nvim plugin entry point
--- This file is sourced automatically by Neovim's plugin loader.

if vim.g.loaded_morg then
    return
end
vim.g.loaded_morg = true

-- The plugin is lazy — setup() must be called by the user.
-- All commands and keybindings are registered in setup().
